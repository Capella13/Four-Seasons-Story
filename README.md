# Four-Seasons-Story

올려야하는 파일 3개가 안 보인다... 코드가 없어서 그런걸까? 일단 *아무거나* 실행파일이라도 올려보자.

로고 리소스 들어오면 그때 업데이트 해보자.



(+수정)

**실행파일만 가지고는 실행이 안된다는 것**을 발견했다. 하지만 **그 파일(Asset, Packages, ProjectSettings)** 이 안 보이기 때문에 어쩔 수가 없다. 포기하도록 하자.
